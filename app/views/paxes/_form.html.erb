<%= form_for(@pax) do |f| %>
  <% if @pax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pax.errors.count, "error") %> prohibited this pax from being saved:</h2>

      <ul>
      <% @pax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.number_field :name %>
  </div>
  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.number_field :room_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
