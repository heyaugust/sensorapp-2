<%= form_for(@storey) do |f| %>
  <% if @storey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@storey.errors.count, "error") %> prohibited this storey from being saved:</h2>

      <ul>
      <% @storey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
